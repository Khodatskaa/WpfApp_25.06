<Window x:Class="WpfApp_25._06.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_25._06"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtStatus" Text="UDP Server is not running." HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 20">
            <Button Content="Start Server" Click="StartServer_Click" Margin="5"/>
            <Button Content="Stop Server" Click="StopServer_Click" Margin="5" IsEnabled="False"/>
        </StackPanel>

        <ListBox x:Name="lstClients" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="20"/>
    </Grid>
</Window>
